import React from "react"

const CommandNotFound = () => {
	return (
		<div className="container container-sm">
			Commmmand not found. Consider contributing to the project.
		</div>
	)
}

export default CommandNotFound
